# NER v8 MITRE API Configuration

server:
  host: "0.0.0.0"
  port: 8000
  workers: 4
  reload: false
  log_level: "info"

model:
  path: "/home/jim/2_OXOT_Projects_Dev/Import 1 NOV 2025/7-3_TM - MITRE/models/ner_v8_mitre"
  version: "v8"
  performance:
    f1_score: 97.01
    precision: 94.20
    recall: 100.00

security:
  # IMPORTANT: Change this in production!
  api_key: "dev-key-change-in-production"
  rate_limit:
    requests_per_minute: 100

cors:
  allow_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
  allow_credentials: true
  allow_methods:
    - "GET"
    - "POST"
  allow_headers:
    - "*"

batch_processing:
  max_texts_per_request: 100
  max_text_length: 50000

monitoring:
  prometheus_enabled: true
  log_requests: true
  log_entities: false  # Set to true for debugging

# Production settings
production:
  host: "0.0.0.0"
  port: 8000
  workers: 8
  timeout: 120
  keepalive: 5
  ssl_certfile: null
  ssl_keyfile: null
