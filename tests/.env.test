# Test Environment Variables for Security Tests
# These are test credentials - NOT production values

MINIO_ACCESS_KEY=test_access_key_for_testing
MINIO_SECRET_KEY=test_secret_key_for_testing
MINIO_ENDPOINT=http://localhost:9000
MINIO_BUCKET=test-uploads-bucket
MINIO_REGION=us-east-1

# Next.js test environment
NODE_ENV=test
